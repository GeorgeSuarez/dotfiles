# Build / VCS noise — never applied to $HOME
**/node_modules/**
**/.git/**
*.log
**/.DS_Store

# Runtime state — lives in $HOME only, never synced back to source
**/sessions/**
**/mcp-cache.json
**/mcp-onboarding.json
*.bak
