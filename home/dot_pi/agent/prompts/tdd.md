---
description: Implement the task test-first using the tdd skill's red-green-refactor loop
argument-hint: "<task>"
---
Use the `tdd` skill to implement: ${ARGUMENTS:-the task we just discussed}.

Follow its red-green-refactor process strictly: write the smallest failing test first, add the minimum code to pass, refactor while tests stay green. Run the full test suite at the end and report the results.
